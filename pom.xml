<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- CLAUDE.md → Stack: Java 21, Spring Boot 3.x. -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.client360</groupId>
    <artifactId>client360-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Client360</name>
    <description>Banking CRM: one auditable view of a customer (SPEC.md).</description>

    <!--
      audit-service joins the reactor with v2 (SPEC.md §11.1). Until then it has no
      buildable sources, and listing it here would only produce an empty jar.
    -->
    <modules>
        <module>src/common</module>
        <module>src/client-service</module>
        <module>src/interaction-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <libphonenumber.version>9.0.39</libphonenumber.version>
        <spotless.version>3.10.2</spotless.version>
        <palantir-java-format.version>2.98.0</palantir-java-format.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.client360</groupId>
                <artifactId>client360-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.googlecode.libphonenumber</groupId>
                <artifactId>libphonenumber</artifactId>
                <version>${libphonenumber.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <!-- Mockito's inline mock maker self-attaches an agent; JDK 21+ warns without this. -->
                        <argLine>-XX:+EnableDynamicAgentLoading</argLine>
                        <!-- Testcontainers singletons are shared across test classes in one JVM. -->
                        <forkCount>1</forkCount>
                        <reuseForks>true</reuseForks>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!-- ./mvnw spotless:apply  — format before committing (CLAUDE.md → Commands). -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <palantirJavaFormat>
                            <version>${palantir-java-format.version}</version>
                        </palantirJavaFormat>
                        <removeUnusedImports/>
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                    </java>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
