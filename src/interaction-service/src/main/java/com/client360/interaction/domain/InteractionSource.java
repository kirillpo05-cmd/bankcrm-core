package com.client360.interaction.domain;

/** {@code interaction.interaction_source} (SPEC.md §6.2.1). */
public enum InteractionSource {
    WEB,
    API,
    TELEPHONY_IMPORT,
    EMAIL_IMPORT,
    MIGRATION;
}
