package com.client360.client.domain;

/** {@code client.product_type} (SPEC.md §5.2.3). */
public enum ProductType {
    CURRENT_ACCOUNT,
    SAVINGS_ACCOUNT,
    DEBIT_CARD,
    CREDIT_CARD,
    MORTGAGE,
    CONSUMER_LOAN,
    TERM_DEPOSIT,
    INVESTMENT_ACCOUNT,
    INSURANCE;
}
